# Analytics Service — Environment Variables
# ML Model: Linear Regression with features [duration_minutes, temperature, hour_of_day]
# Training data: simulated (12 samples)
#
# No environment variables required locally.
# In production, model file path can be set here:
# MODEL_PATH=models/energy_model.pkl
